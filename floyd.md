Floyd Warshall
Dijkstra는 S,E가 정해져있다. 그래서 1차원 배열만 상태를 업데이트하면 됨.
근데 모든 지점들에 대한 경로를 알고 싶을 수 있다. 이러면 어떻게 해야하나? 2차원 배열을 만들면 된다.
2차원 배열에 대해서 DP를 수행할 거임. 이 때 DP를 정의하는 상태들은 X->Y로 간다는 거리. 마찬가지로 확정된 set과 그렇지 않은 set으로 나누고, 그렇지 않은 set에서 확정 set으로 넘기는 작업을 진행한다고 생각하면 된다.
다만 dijkstra처럼 확정 set을 따로 관리하지는 않고, 계속해서 min 형식으로 줄여나가는 것임.

기본적인 아이디어는 X->Y로 가는 경로에서 X->A, A->Y로 가는게 더 빠르다면, X->A->Y가 최단이다 라는 걸 비교하면 되는 식이다. 따라서 3중 for-loop을 돌면서 2차원 배열을 채워나가야된다는 생각을 할 수 있음.
그리고 이 때 루프 순서는 경유지 - 시작 - 끝점이 맞다.
왜냐하면 경유지가 오픈되었을 때를 기준으로 문제가 독립적으로 나뉘어지기 때문이다.
모든 (출발, 도착) 쌍들에 대해서 경유지를 하나씩 오픈하는 것들은 순서에 상관이 없다. 경유지를 하나씩 오픈을 해서 (출발, 도착)쌍들이 해당 경유지를 써서 최단거리가 나오느냐 나오지 않냐는 계산해보고 나서 저장하는 것이다. 
반면 시작점 - 끝점 - 경유지로 하면 생기는 상황은 다음과 같다.
시작점에서 끝점으로 가는 길을 만드는데, 경유지를 거쳐 가봐라는 뜻이다. 경유지에서 시작해서 끝점으로 가는 거나, 출발지에서 시작해서 경유지로 가는 경로가 이미 연결되어있을 때는 말이 되지만 그렇지 않다면 말이 되지 않는다. 그리고 이건 (출발지, 경유지), (경유지, 끝점) 경로처럼 loop 바깥쪽 2개의 변수가 포함된 조건이고, 조건부 조건이라 독립적이지 않음. 

### 경로 역추적
Floyd Warshall은 d[i][j] = d[i][k] + d[k][j] 이다. 
따라서 탐색하는 방식도 경유지 기준으로 열면서 반복문을 돌린다.
그럼 경로는 예를 들엇 d[i][j]의 경로는 d[i][k] + d[k][j] 들 중 min이기 때문에 어떤 경유지 k를 거쳐서 왔는지 알면 된다. 따라서 via 방식으로는 각 칸에 이 경로의 경유지가 어디인지를 k를 기록해놓으면 다시 i->k와 k->j 칸을 찾아서 recursive하게 추가할 수 있다.
또 다른 방법으로는 next 방식이 있다. next 방식이라고 별게 있는거 같지는 않고, 똑같이 d[i][j] = d[i][k] + d[k][j]이기 때문에 d[i][k]를 그 칸에 기록해놓는 것이다. 이게 가지는 의미는 결국 i에서 j로 가려면 k를 거쳐야하니 d[i][k]로 가는 방향으로 갈 때 내딛는 첫걸음을 거기에 적어놓아라는 것이다.
floyd 방식은 경유지가 여러 개일 때, 앞의 경유지에서는 최단 경로가 발견되지 않다가 뒤의 경유지에서 최단경로가 이어지면서 발견될 수 있다. 하지만 모든 경우에서 d[i][j] = d[i][k] + d[k][j]로 업데이트를 하기 때문에 거치는 경유지 k1, k2, k3에 대해서 합은 모두 같다.

