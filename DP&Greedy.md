DP 는 뭔가... 그냥 감이야..? 어떻게 체계적으로 사고해야하는거야?

이진 탐색으로 조건 만족 개수 찾는 방법 설명하기.
이진 탐색으로 조건 만족 개수 찾는 방법 설명하기.
핵심 아이디어는 정렬된 배열은 "조건을 만족하는 앞부분"과 "만족하지 않는 뒷부분"으로 딱 나뉜다는 거예요. 이분 탐색은 그 경계를 반씩 좁혀가며 찾는 방법이에요.

무엇을 찾는가
회의 문제에서 필요한 건 "끝나는 시간이 s 이하인 회의가 몇 개냐"였죠. 끝 시간 배열이 정렬돼 있으면 이런 모양이에요.

ends = [2, 4, 5, 7, 9, 10, 12, 15],  s = 8

        2  4  5  7 | 9  10  12  15
        ≤8 ≤8 ≤8 ≤8 | >8 >8  >8  >8
                    ↑ 경계
경계의 위치(여기서는 인덱스 4)가 곧 "s 이하인 개수"예요. 앞에서부터 하나씩 세면 O(n)인데, 정렬돼 있으니 훨씬 빨리 찾을 수 있어요.

반씩 좁히기
업다운 게임이랑 같아요. 가운데 하나를 찍어보면 그 한 번의 비교로 절반이 확정돼요.

가운데 값이 s 이하면 → 그 왼쪽은 전부 s 이하(정렬됐으니까). 경계는 오른쪽 절반에 있음
가운데 값이 s보다 크면 → 그 오른쪽은 전부 s보다 큼. 경계는 왼쪽 절반에 있음
python
def count_le(ends, s):
    lo, hi = 0, len(ends)
    while lo < hi:
        mid = (lo + hi) // 2
        if ends[mid] <= s:
            lo = mid + 1   # mid까지는 전부 ≤ s로 확정
        else:
            hi = mid       # mid부터는 전부 > s로 확정
    return lo
lo, hi의 의미를 이렇게 잡으면 안 헷갈려요.

lo 왼쪽: ≤ s 확정
hi부터 오른쪽: > s 확정
[lo, hi): 아직 모르는 구간
모르는 구간이 매번 절반으로 줄고, 비면(lo == hi) 그 자리가 경계예요.

실제로 돌려보면
ends = [2, 4, 5, 7, 9, 10, 12, 15], s = 8

단계	lo	hi	mid	ends[mid]	판단
1	0	8	4	9	9 > 8 → hi = 4
2	0	4	2	5	5 ≤ 8 → lo = 3
3	3	4	3	7	7 ≤ 8 → lo = 4
끝	4	4			답 4
8개짜리를 3번 비교로 끝냈어요. n개면 약 log₂n번이라, 100만 개여도 20번 정도예요.

bisect_right와의 관계
파이썬의 bisect_right(ends, s)가 정확히 위 함수예요. 비슷한 bisect_left는 조건이 < s라서, s와 같은 값은 세지 않아요.

회의 문제에서는 "끝나는 직후에 바로 시작할 수 있다"고 했으니, 끝 시간이 시작 시간과 같은 회의도 앞에 올 수 있어요. 그래서 같은 값까지 세는 bisect_right를 쓴 거예요. ends=[2, ...]이고 [2,9] 회의의 시작이 2일 때, [1,2]를 포함해서 p=1이 나온 게 이 덕분이에요.

끝시간 이하인게 여러 개 나오면 그럼 어떻게 되

Untangling how overlapping meeting end times are handled.
Untangling how overlapping meeting end times are handled.
여러 개 나와도 괜찮아요. p(i)는 "그중 하나를 고른다"가 아니라 **"여기까지가 후보 구간이다"**라는 경계일 뿐이고, 그 안에서 어떻게 고를지는 이미 dp[p(i)]가 해결해 뒀어요.

예시
끝나는 시간 순으로 정렬된 회의 5개가 있다고 해요.

번호	회의
1	[1, 2]
2	[1, 3]
3	[2, 4]
4	[3, 5]
5	[6, 7]
5번 [6,7]은 6에 시작하니까, 앞의 4개가 전부 6 이하에 끝나요. 그래서 p(5) = 4예요.

그런데 이 4개를 다 쓸 수 있는 건 아니에요. 자기들끼리 겹치거든요. 여기서 헷갈리실 수 있는데, 그 4개 중 서로 안 겹치게 최대 몇 개를 고를 수 있냐는 질문의 답이 바로 dp[4]예요. 이미 앞 단계에서 계산해 둔 값이죠.

dp를 채워보면

dp[1] = 1 ([1,2])
dp[2] = 1 ([1,2]와 [1,3]은 겹쳐서 하나만)
dp[3] = 2 ([1,2], [2,4])
dp[4] = 2 ([3,5]를 넣어도 [1,2], [3,5]로 여전히 2)
dp[5] = max(dp[4], dp[4] + 1) = 3 (앞 4개 중 최적 2개 + [6,7])
즉 "i를 쓴다"를 고르면 p(i)개의 후보 안에서의 최적해를 통째로 가져와서 +1하는 거예요. 후보가 1개든 100개든 dp[p(i)] 한 번 참조로 끝나요. 편집 거리에서 "마지막 열을 떼면 남는 작은 문제의 답은 이미 표에 있다"던 것과 같은 원리예요.

혹시 이분 탐색에서 같은 값이 여러 개인 경우를 물으신 거라면
ends = [2, 3, 3, 3, 5], s = 3이면 bisect_right는 4를 돌려줘요. 3이 여러 개여도 전부 넘어간 자리를 찾아요. 코드에서 ends[mid] <= s면 lo = mid + 1로 오른쪽으로 밀기 때문에, 3과 같은 값을 만나면 계속 오른쪽으로 가서 결국 마지막 3 바로 뒤에서 멈춰요. 그래서 끝 시간이 시작 시간과 같은 회의들이 여러 개 있어도 빠짐없이 후보로 세어져요.

그럼 이걸 애초에 끝나는 시간으로 정렬하겠다는 아이디어가 있어야하네?

Explaining why sorting by end time enables the algorithm.
Explaining why sorting by end time enables the algorithm.
네, 필요해요. 다만 번뜩이는 발상이라기보다는 DP 상태를 단순하게 만들려다 보면 자연스럽게 도착하는 아이디어예요.

정렬 안 하면 무슨 일이 생기나
처음에 말한 핵심 질문을 다시 떠올려 보세요. "미래를 결정하는 데 과거의 무엇이 필요한가?"

회의가 아무 순서로 섞여 있으면, 다음 회의를 넣을 수 있는지 판단하려면 지금까지 고른 회의들의 목록 전체를 봐야 해요. 어떤 시간대가 비어 있는지 알아야 하니까요. 상태가 "고른 회의 집합"이 되면 경우의 수가 2ⁿ이라 DP가 안 돼요.

정렬은 이 과거 요약을 인덱스 하나로 압축하려는 장치예요. "i와 같이 쓸 수 있는 회의들은 앞쪽 연속 구간"이 되게 만들면, 상태가 dp[i]로 끝나거든요. 그러니까 발상의 순서는 "끝 시간으로 정렬하자"가 먼저가 아니라, **"과거를 연속 구간으로 만들려면 어떤 순서가 필요하지?"**가 먼저예요.

왜 하필 끝 시간인가
접두사 사고로 "마지막으로 넣은 회의 i"를 기준으로 보면, 그 앞에 올 수 있는 회의의 조건은 **end ≤ start_i**예요. 조건에 등장하는 게 앞 회의들의 끝 시간이니까, 끝 시간으로 정렬해야 조건을 만족하는 회의들이 앞쪽에 모여요. 즉 전이 조건에서 "이미 결정된 쪽"이 쓰는 값으로 정렬하는 거예요.

시작 시간으로 정렬해도 풀려요
재미있는 건, 접미사 사고로 가면 시작 시간 정렬이 자연스러워진다는 거예요.

f(i) = 시작 시간 순 i번째 회의부터 끝까지 고려했을 때 최대 개수

안 쓴다: f(i+1)
쓴다: 이 회의가 끝난 뒤 시작하는 첫 회의 k를 찾아서 1 + f(k)
이번엔 조건이 start_k ≥ end_i이고, "앞으로 올 쪽"이 쓰는 값이 시작 시간이니까 시작 시간으로 정렬하면 뒤쪽 후보들이 연속 구간이 돼요. k도 시작 시간 배열에서 이분 탐색으로 찾고요.

예시로 확인하면, 시작 순 [1,2], [2,9], [3,6], [4,8]에서

f(4) = 1, f(3) = 1, f(2) = 1
f(1) = max(f(2), 1 + f(2)) = 2 ([1,2]가 끝난 2 이후 첫 회의가 2번)
답 2로 같아요.

정리
그러니까 외울 건 "끝 시간 정렬"이 아니라 이 두 가지예요.

구간·일정 문제면 정렬부터 의심한다. 정렬하면 과거(또는 미래)가 연속 구간이 되고, 상태가 인덱스 하나로 줄어들 가능성이 커요.
정렬 기준은 전이 조건에서 읽는다. 접두사 사고면 앞 회의의 끝 시간이, 접미사 사고면 뒤 회의의 시작 시간이 조건에 나오니 그걸로 정렬해요.
단, 그리디는 얘기가 달라요. 그리디는 교환 논증이 성립하는 기준이 끝 시간뿐이라서, 그리디로 풀려면 끝 시간 정렬이 필수예요. DP는 어느 쪽으로도 풀리지만 그리디는 기준 하나만 맞는 거죠.
