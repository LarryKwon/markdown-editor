## Kruskal & Prim
### 알아야하는 자료 구조(Union-Find)
Union-Find는 특정 원소가 어디 속하는지 찾거나 (Find), 속하는 그룹을 합칠 때(Union) 사용하는 것이다.
예를 들어 각 사람별로 그룹이 정해져있는데, 1) 사람이 어디 속하는지 2) 그룹에 누가 있는지 3) 그룹을 합치기 상황을 가정해보자.

이 때 1) 일반 배열을 쓰는 경우 2) linked list를 쓰는 경우로 나누어서 시간복잡도가 어떻게 되는지 우선 보면

일반 배열을 쓰는 경우, 배열의 index를 원소의 id로 놓을 수 있다. 그리고 element를 원소의 그룹 id로 놔둔다.
이런 경우 find는 쉽다. 사람이 어디 속하는지는 index로 찾기 때문이다. 이제 그룹1에 해당하는 사람들을 찾아다가 그룹2로 합치고자 한다. 이러면 문제다. 모든 원소를 돌면서 그룹1인지 아닌지 순회해야하기 때문에 O(n)이 된다.

linked list를 쓰는 경우, 각 사람이 node다. 같은 그룹이라면 linked list로 엮고, head에는 그룹의 특성만 붙인다. 이건 반대로 특정 사람이 어디 속하는지 찾기가 힘들다. 사실상 순회를 해야하므로 O(N)이다. 하지만 합치기는 쉽다. head만 찾아서 다른 그룹에다가 이어붙여주기만 하면 되기 때문이다.

이렇게 2가지 자료구조는 각각 find,union에 대해서 밸런스 있는 성능을 내지 못한다. union-find는 이 점을 해결한다.
주요한 방법은 tree 구조를 사용하는 것이다. 연결리스트처럼 한 줄로 세워서 병합은 쉽게 하되, 트리 구조로 넓게 뭉치게 만든다. 
Union Find에서는 모든 노드가 자신의 부모나 기억하게 만든다. find()는 재귀적으로 탐색하면 되고, union은 그룹장들 간에 또 위계를 만들면 된다.

### 알아야하는 문제
MST - Minimum spanning Tree
모든 정점 N개를 잇는 간선 N-1개를 만들 때, 가장 적은 weight로 만드는 방법

Krsukal
- MST 찾는 알고리즘임.
간선이 작은 것부터 이으면서, greedy하게 가고 최소로 이으면 되는거 아닌가? => greedy 알고리즘 맞다.
'당장의 최선'이 '전체의 최선'으로 이어지는 케이스.

교환 논법으로 증명됨. 매 순간 최선의 선택을 하는게 왜 전체의 최선으로 이어지는지.

그래서 간선을 정렬해서 하나씩 뽑는데, Union-find를 이용해서 find를 사전에 해서 같으면 cyclic이므로 잇지 않고 skip, 다르면 acyclic이므로 잇고 union 연산 진행.
기본적으로 배열로 구현됨. greedy하게 간선 뽑기 때문에 간선에 대한 정렬 필요


Prim
- MST 찾는 알고리즘임.
어느 점에서 시작하든 상관없음. 당연한게 MST이기 때문에 어차피 어떤 vertex든 다 포함하고 있기 때문.
Prim 구조 상, 어떤 점에서 시작하든 이어질 수 있는 length의 최소값으로 되어있는 node를 뽑는 식임.
그러다보니 priority queue를 사용하고 dijkstra랑 구조가 같음. 

