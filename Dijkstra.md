# Dijkstra
### 다익스트라

다익스트라는 사실상 Greedy + DP 개념이다.
다익스트라는 다음과 같은 컴포넌트와 순서로 진행된다.
컴포넌트
- 거리 matrix
- 시작점에서 각 점까지의 거리 table <- 이게 dp로 업데이트하는 상태 테이블이다.

1. 먼저 상태 테이블에서 초기화를 해줘야한다.
초기화는 시작점을 제외한 나머지 다른 점을 전부 무한대로 만드는 것.
그럼 이제 상태 테이블은 2가지 케이스로 나뉜다. 짧은 거리가 확정된 set과 그렇지 않은 set
이제부터 최단 거리가 확정되지 않은 set에서 확정된 set으로 하나씩 넘길 것이다.
2. 현재 테이블에서 아직 방문하지 않은 친구들 중에 minimum 값을 뽑는다.
3. 뽑은 뒤에 연결되어있는 간선을 기준으로 해서 다른 점들의 거리를 업데이트 시킨다.
4. 이걸 반복하면서 뽑힌 경우는 최단거리 확정 set으로 넣는 식이다.

이게 통하는 지점은 다음과 같다.
1. 기본적으로 최단거리는 이 때까지 확정된 최단거리에서 최단거리를 구하는 부분문제로 풀린다.
2. 확정 set들로부터 간선의 정보를 이용해서 거리를 새롭게 구해내는 것은 monotonic하다. 따라서 줄어들 일은 없다.
3. 거리 정보를 업데이트한 set에서 하나를 뽑는 것은 확정 set으로 옮길 수 있다. (왜냐하면 그 경우가 최단이 아닌 다른 경우가 존재한다고 하면, 그건 확정 set을 거치지 않는 다른 경로, 즉 확정 set이 아닌 정점에서 더해져서 들어오는 경로일텐데, 이 경우는 항상 더 크다.)

구현 방법은 다음과 같다.
- priority queue를 이용해서 확정되지 않는 set에서 min을 찾아 확정 set으로 바꾼다. min만 찾는 문제이므로 priority queue를 이용한다.
- 간선 정보를 이용해서 상태를 업데이트하고 priority queue를 업데이트한다.
따라서 간선을 모두 들르기 때문에 O(E)이고, 한 간선을 뽑을 때마다 상태 업데이트 및 min 값 정렬을 위한 O(Log V)가 발생한다. 따라서 전체 시간복잡도는 O(ELogV)이다.

심플하게 구하는 방법으로는 상태 테이블을 모두 돌면서 min을 구하는 방법이 있는데, 이 경우 min을 구하는데 O(V)가 걸린다. 그리고 전체 알고리즘을 돌 때, 각 정점별로 이제 돌기 때문에 O(V)라서 O(V^2)가 된다.
